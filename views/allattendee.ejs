<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%=title%></title>
    <link rel="stylesheet" href="/css/bootstrap.min.css">
</head>
<body class="">
    <h1>The Farewell Party Project</h1>
    <h3>All Attendees</h3>
    <!-- Generate a List of attendees -->
    <ul>
        <% for (var i = 0; i < data.length; i++) { %>
            <% if (data[i].payment.status) { %>
                <li><a href=<%= "/getMyProfile/"+data[i]._id %>><%= data[i].name %></a>(Paid)</li>
            <% } %>
        <% } %>
        <% for (var i = 0; i < data.length; i++) { %>
            <% if (!data[i].payment.status) { %>
                <li><a href=<%= "/getMyProfile/"+data[i]._id %>><%= data[i].name %></a>(Payment Incomplete)</li>
            <% } %>
        <% } %>
    </ul>

</body>
</html>